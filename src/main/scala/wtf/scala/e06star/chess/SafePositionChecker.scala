package wtf.scala.e06star.chess

class SafePositionChecker extends PositionChecker {
  // todo are given set of placed pieces safe?
  override def apply(board: Set[ChessPiecePosition]): Boolean = ???
}